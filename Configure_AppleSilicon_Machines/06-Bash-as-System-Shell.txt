#
# Make bash your system shell instead of zsh
#

# Change the system shell

chsh -s /bin/bash

# Configure profiles and aliases
# The settings for brew are already incorporated.
# It just was:
# 	eval "$(/opt/homebrew/bin/brew shellenv)"
# which expands to
# #>
# export HOMEBREW_PREFIX="/opt/homebrew";
# export HOMEBREW_CELLAR="/opt/homebrew/Cellar";
# export HOMEBREW_REPOSITORY="/opt/homebrew";
# export PATH="/opt/homebrew/bin:/opt/homebrew/sbin${PATH+:$PATH}";
# export MANPATH="/opt/homebrew/share/man${MANPATH+:$MANPATH}:";
# export INFOPATH="/opt/homebrew/share/info:${INFOPATH:-}";

# Link profiles and aliases

# Adapt the name of the root directory of the checked out public git repo for GdP1
# (here git_public_GdP1)

cd $HOME

ln -s git_public_GdP1/Configure_AppleSilicon_Machines/Bash/dot.bashrc .bashrc

ln -s git_public_GdP1/Configure_AppleSilicon_Machines/Bash/dot.profile .profile

ln -s git_public_GdP1/Configure_AppleSilicon_Machines/Bash/dot.bash_aliases .bash_aliases

#
# Important:
# Logout and then Login again
#
# Your login shell and your profiles are only activated during login!


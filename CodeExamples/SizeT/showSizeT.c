#include<stdio.h>
#include<stdlib.h>

int main() {
        size_t thesize;
	printf("Anzahl Byte für thesize ist %d\n",sizeof(thesize));
	printf("Anzahl Byte für size_t ist %d\n",sizeof(size_t));
        return 0;
}
